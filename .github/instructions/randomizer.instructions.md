---
applyTo: "src/randomizer/**/*.py"
---

# Randomizer Instructions

- Randomizer behavior must stay deterministic and pure.
- Do not import GUI modules.
- Do not add filesystem side effects unless the approved scope explicitly requires them.
- Keep randomizer rules aligned with PromptPack and builder-pipeline semantics.
- Mirror changes in `tests/randomizer/`.
