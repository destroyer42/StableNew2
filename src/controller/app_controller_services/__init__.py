"""app_controller_services — extracted service helpers for AppController.

These modules hold cohesive logic extracted from AppController. They have
explicit, typed inputs and outputs and carry no hidden GUI dependencies.
AppController delegates to them rather than inlining the logic.
"""
