"""learning_controller_services — extracted service helpers for LearningController.

These modules hold cohesive logic extracted from LearningController. They have
explicit, typed inputs and outputs with no hidden GUI dependencies.
LearningController delegates to them rather than inlining the logic.
"""
